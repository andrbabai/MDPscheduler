{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build ICS",
            "type": "shell",
            "command": "python -m schedics.cli build --config config.yml --output public/schedule.ics"
        },
        {
            "label": "Run Server",
            "type": "shell",
            "command": "uvicorn schedics.server:app --reload --port 8080"
        }
    ]
}
